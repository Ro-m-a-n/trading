export const Stage = (props) => {
  return (
    <div className="stage_wrap">
      <div className="stage_number">{props.number}</div>
      <h3>{props.title}</h3>
      <p>{props.text}</p>
    </div>
  );
};
2;
